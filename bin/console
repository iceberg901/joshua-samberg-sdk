#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "dotenv/load"
require "the_one_api"

Client = TheOneApi::Client.new(ENV["THE_ONE_API_KEY"])

require "irb"
IRB.start(__FILE__)
